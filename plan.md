# Learning English in Singapore · 实施计划

版本：1.1 · 2026-09-11

**当前状态：用户已批准项目初始化及第一批T01–T06。** Git、运行状态与项目hooks已接通，已核实T01–T06六项真实绿存档；B1进入Stage5验收，动态状态见tasks.md。下文为已审阅实施计划，模型、登录、视频速度和学习效果仍未验证。初始化记录见 `docs/setup-status.md`。

## 1. 依据和交付目标

产品依据为 `spec.md` v0.2，SHA-256：`717c700682b5803c14673204512d06cef7158941b487002b21311b7173dff843`。沿用两份痛点报告、73 项技术来源、36 个场景种子、6 个完整设计样例、40 条用户诉求和 **73 条功能验收句**。来源 `T01–T73` 属于研究编号；本计划的 `任务 T01–T40` 是开发编号，不能混用。

四份文件的职责：`spec.md` 说明做成什么；`rules.md` 说明不可牺牲什么；本文件说明怎样实现、验证及处理失败；`tasks.md` 说明下一步、依赖和完成证据。原规格中的“本次只交付第一份文档”描述上一轮交付，不限制本轮补齐配套文件；本次保持已确认规格原文和 PDF 不变。

完成首版的含义是：符合服务地区的成人可以在手机上用微信或 Google 登录，经自然选择了解起点，用母语提出真实沟通目的，听按能力生成的新加坡英语双角色场景，反复听同一材料，再与同一个角色进行 AI 主动引导的开放练习；保存进度、纠错和删除均可用。是否改善迁移和压力，由单独试点观察，不能由技术完成推定。

## 2. 实施顺序与可见成果

总计 40 项，分 7 批。当前所有任务未开始。**第一批 6 项先形成可操作的本地流程和可执行的验证方案，不接真实账号或付费模型。** 原型中未接通的部分持续标明“流程演示／待接入”，不出现伪造的生成成功、登录成功或已掌握评分。

| 批次 | 阶段／任务 | 用户能看到什么 | 批末退出条件 |
| --- | --- | --- | --- |
| B1 | M0 准备，T01–T06 | 手机布局、辅助语言、逐题引导、场景确认和两模式交互原型；完整实验清单 | 6 项离线证据齐；真实能力全部仍标未测 |
| B2 | M0 实测，T07–T12 | 同机登录、本地声音和真正生成的双角色短课，在目标手机上演示 | G1–G4 基础门槛逐项通过；否则记录失败，不进入依赖该能力的完整构建 |
| B3 | M1，T13–T18 | 个人场景从需求生成到固定课件、循环听和 AI 主动开场 | 样例 A 主流程可操作，未知反馈与故障支撑保留 |
| B4 | M1，T19–T24 | 可以改变目标、求助、纠错、断线续练、看回顾和删除 | 动态会话、私有记录、退出／删除生命周期可验证 |
| B5 | M1，T25–T29 | 一条家长与老师场景形成可重复验证的完整闭环 | 该路径 F1–F14，G5 会话与评分校准在此范围内通过 |
| B6 | M2，T30–T35 | 六个完整样例、36 个入口、列表外任务、已审核辅助语言的完整首版候选 | 73 条功能验收、G1–G5 全范围复核及内容／设备矩阵通过 |
| B7 | M3，T36–T40 | 成人探索试点、失败样本修正和可追溯的学习结果报告 | G6 给出继续／修正／缩小验证范围决定；不自动公开上线 |

★ T01 是首个可点开的本地版本；★ T12 是技术可行性节点；★ T29 是单场景闭环；★ T35 是首版候选；★ T40 是试点决策。里程碑不自动包含发布授权。

### 2.1 工时如何理解

`tasks.md` 每项同时给人类团队工时和 AI 协作工时，均为**投入人时的规划区间**，不是供应商承诺或实测生产率。AI 协作工时含工程师审查、操作与修正；当地语言审查、参与者沟通和真机操作不假设由 AI 替代。

总量由任务区间相加，见任务表末尾；首个可点击版本为 T01 的区间。跨任务可以并行，但不能直接将总人时除以人数当上线日期。账号审批、预算落实、招聘审查者、样本招募、供应商排队，以及 3 次分散练习和 48–72 小时延迟观察，另计日历等待，AI 不能压缩。M0 后依据实测重新估算后续批次。

## 3. 为什么这样选技术

| 层 | 本计划建议 | 理由与边界 |
| --- | --- | --- |
| 手机界面与业务服务 | Next.js App Router + React + TypeScript，先做响应式网页 | 无需安装 App；页面、登录回调和个人记录可在一个 Web 项目维护。框架支持移动浏览器，不代表实时媒体已过真机验收。[工程依据 E01](https://nextjs.org/docs/app/getting-started/installation) |
| 语音／视觉会话 | Python LiveKit Agents，OpenAI Realtime 插件 + Runway 首测，Simli 对照 | 有现成音频和视频适配器；把长时间媒体连接放在持续运行的 worker，不塞进普通网页请求。精确组合仍由 M0 证明。[E02](https://docs.livekit.io/agents/start/voice-ai-quickstart/)；spec 来源 T41–T45 |
| 场景和教学内容 | 服务端 OpenAI Responses + 结构化合约，独立校验 | 模型生成适合目的的内容，程序管目标、版本与合法状态；不依赖一段巨大提示词控制全部行为。模型候选沿用 spec §10，首次接入重核可用性。 |
| 固定示范与视觉 | OpenAI 示范音轨 + 经实测的生成式角色；LTX v2 音轨短片用于必要动作镜头 | 声音是教学依据，视频围绕同一声音生成。先验证可录存的片段、音轨和时间轴，再承诺重听；不以直播流替代固定课件。 |
| 记录与文件 | PostgreSQL 保存身份、状态和版本；私有对象存储保存音视频 | 结构化记录和大媒体分开管理，便于鉴权、重试、版本保留和删除。这是工程选型建议，云厂商、地区、价格及保留策略在 T06 的清单中核定。 |
| 验证 | 浏览器流程用 Playwright；媒体 worker 的确定性行为用 pytest；TS 规则使用离线单元测试 | 分开验证可重复程序行为与真实媒体／教学质量。Playwright 模拟设备不能替代真机 OAuth 和声学测试。[E03](https://playwright.dev/docs/intro) [E04](https://docs.pytest.org/en/stable/) |

以上新增工程资料于 2026-09-10 核查官方入口；不重新声称已对整个技术生态做过最新横向测试。精确依赖版本在 T01/T06 根据运行时与插件兼容性锁定，记录 lockfile 和来源；不在现在猜 Node、Python 或 SDK 的已安装版本。

不同时引入多个主会话框架，不默认微调模型，不自建视频基础模型训练集，不启动研究级多 GPU 路线。必要的内部实现选择由工程负责人决定并记录；更换 OpenAI 语音、取消生成式视频或削弱产品门槛，属于产品变更。

## 4. M0：把最大未知先验证

### 4.1 进入真实实验前的条件

T06 先做好可审阅的实验包，而不是直接索取 API Key：账号／主体能力清单、数据流、版本与许可、每个实验样本数、官方单价核查日期、费用上限建议、取消方式、审查者任务、证据表和测试环境变更清单。不得要求把密钥粘到聊天里。

| 条件 | 在哪一步前落实 | 未满足时的行为 |
| --- | --- | --- |
| A0 具体批次实施授权 | T01 前 | 当前仅交付文档；不写应用或执行循环 |
| A1 项目 Git／运行状态与 hooks 的范围 | T01 中准备，启用前落实对应授权 | 可按已批准范围建立本地代码与验证；不直接执行会写 AGENTS.md／`.codex` 的通用脚本；未安装 hooks 时只做有界人工监督循环 |
| A2 实验预算和服务账号权限（D01/D05） | T08–T10、后续真实生成前 | 完成离线替身与实验包；真实调用标待执行，不能计为通过 |
| A3 微信／Google 主体、回调域名、授权测试环境（D04） | T07 前 | 不以假 token 或 PC 二维码通过 G1；账号申请和环境发布另有授权 |
| A4 辅助语言组合与外部联系授权（D02） | T08 本地审查、T11 观察前 | 默认规划简体中文／印尼语／日语及英语界面；未获确认的语言不宣称正式支持，不擅自发招募信息 |
| A5 供应商数据处理及参与者同意（D06） | 任一真实参与者进入外部数据链前 | 只用虚构场景、合成角色和获授权声音；供应商未知条款列出，不承诺零留存 |
| A6 私有试点部署与研究预算／联系人 | T36–T38 前 | 可以完成发布清单和研究材料，不发布、不联系、不收集真人录音 |

授权一经明确，在已限定环境、预算和批次内有效，不逐次重复询问。实际 API 和凭据能力在安全环境中核验，不在文档填“已具备”。

### 4.2 实验与可比性

| 实验 | 固定输入、操作与最小证据 | 放行／失败决定 |
| --- | --- | --- |
| X1 登录 | 微信内、手机 Safari/Chrome、电脑分别记录实际主体权限、同机授权、取消重试、回调失败、Google 外部浏览器接续及语言保持 | G1 必须有手机实录和脱敏回调证据；缺同机路径不能进入承诺两入口的首版 |
| X2 声音 | 30 段示范 + 30 段实时同角色配对；慢速／自然速、正式／非正式；两名不同背景本地审查者，分歧第三人复核 | G2 的可理解、场合适配、本地真实性、自然度、跨模式连续性五项分别均分 ≥4/5；任一样本单项 ≤2 须修复复核 |
| X3 视频候选 | Runway 与 Simli 用同一 OpenAI 音轨和相当角色；LTX 用相同长度／分辨率短片。每路线 30 个新任务，冷热和新旧演员分层；每路线 10 个连续 2–5 分钟课件 | 判断不同路线是否满足各自用途，不把异步短片和直播首帧放在一个“最快”排名；失败保留分母 |
| X4 最终媒体链 | 最小真实“确认新目的→OpenAI规划→脚本校验→新OpenAI音轨→视频→手机播放”探针；最终候选至少100次新启动；A→B→A、切同角色对练、打断、30句／30动作、固定录存重播；10次离开+10次断线 | G3 同时满足端到端新视频启动、持续、动作、音画、双角色、课件固定及供应商结束；预制音轨只能用于X3，不可代替X4端到端计时 |
| X5 零基础形成性观察 | 建议 6 位成人，华语近零基础 3 位、非华语近零基础 3 位；可听说明、选择、跳过、求助、只听；研究员不代操作 | G4 基础门槛：六人均能独立到首个理解活动并请求帮助；如有人失败，修正后增加新参与者复核同路径。这是可用性门槛建议，不是代表性结论；M3 仍用24人验证 |

X5 的人数及六人门槛是本计划新增的**形成性验证建议**，需在 A4 中落实，不能冒称来自原痛点统计。只有批准后的实际观察能填通过。M0 演示用最小真实管线，M1 再完善账号隔离、业务恢复、内容运营和完整产品；M0 原型通过不意味着 F1–F14 全部完成。

T06负责核实技术实验的数据范围，T09/T10负责验证M0实际端点、房间订阅、录音／转写、日志、留存、删除与参与者告知；T10保存最小D06检查结论，T11必须引用该结论再开展真人观察。不能等待T24才第一次核验隐私；T24是在完整产品上补齐与复核。T08如使用真实授权声音样本，也须先完成该样本对应端点的数据核验。

X3 初筛可以分步取样，明显不合格就停止，未完成的路线写“淘汰／未达到完整分母”，不能写通过。任一可能作为默认路线的候选必须补足对应完整矩阵。X4 的 100 次也要分条件报告分母；小分层不足以给稳定 P95 时明确样本不足，不把混合总体当单条件 SLA。

### 4.3 速度目标和真实计时

继承 spec §8.5，不静默修改：有意义引入 P95≤2秒；已有相关画面 P95≤3秒；本次新生成、可学习的首段 5–10 秒内容，已有演员 P95≤10秒，新演员 P95≤20秒；连续 2–5 分钟不得出现超过1秒的非教学等待。互动从用户确认结束到**实际听到**回应 P50≤1.5秒、P95≤3秒；音画偏移≤200ms；打断停止 P95≤300ms。完整课件固定保存的 P50/P95 单独报告。

每条记录包含场景确认时间、规划／排队／建角／声音／首帧／首段通过／实际播放／整课固定时间、设备、网络、冷热、演员状态、模型版本、并发、重试与失败。用户思考时间另列。30 秒无新视频就提示听力先行、取消或重试，当前任务状态仍准确；不能显示假进度倒计时。

各条件同时报告“成功请求的时延分布”和“全部启动在门槛内完成的比例”。对该条件的正常启动性能样本，未生成、超时和转只听都记未按时，计算放行分位数时视为无限延迟；至少95%的全部启动须在对应P95门槛内，不能只算成功子集。样本不足或取消导致无法确定分位数时标未能放行。专门的取消／故障实验在实验前分组并单报，禁止看到结果后移出失败样本。

M0 若发现组合不可行，产出哪项失败、同输入对照、可选调整及对用户体验影响。可以在已批准预算内换视觉候选；不能降低本地语音真实性、去掉第二角色、回退固定课件并宣告完成。M1 中不依赖失败能力的工作可以准备，依赖该门槛的任务必须等待结论。

## 5. M1：一条完整且可恢复的学习路径

首条采用 spec 样例 A，但场景合约从第一天支持所有已确认目的。过程为：辅助语言 → 登录 → 逐题认识 → 确认目的 → 准备对话与媒体 → 听同一版本 → 自愿对练 → 支撑／变化／纠错 → 听力优先回顾 → 保存或删除。

### 5.1 认识起点

实现确定性 `placement_rules_v1`，10–14 核心题、最多4附加题。自报经历只作先验；听音选择、支持程度、跳过和设备故障分开存。每个等级按前置档和不同题／主题的独立证据判断，冲突追加同档题最多一次，不足时保留未知。必须覆盖全跳过、偶中高阶、同题多标签重复计数、L2 冲突退至有证据 L1 等反例。

初版界面先可操作，正式音频与图卡在 G2 和教学审查后替换原型占位。L4 材料需两个已审查本地声音；前端不能因为选了“熟练”就直接写 L4。回到上题后保留首次选择、答案播放和支撑记录，不能改选洗掉提示或重复累计同题证据。口语未观察始终单列，用户可调慢、调简单、撤回自动建议。

### 5.2 自由场景与稳定材料

规划器从已审核种子和用户描述产生 3–5 个候选，首屏最多展示3个；目标卡说明谁、何处、希望办成什么和担心点。未知事实只问一个必要问题或保留未知，不要求真实孩子、学校或病历。确认后产生 `goal_revision`，生成脚本按角色、意图、难度、母语支撑和动作时间轴校验。

先校验首章脚本后发音轨，再生成视觉；后续章节滚动准备。每句只播放已校验且已持久记录的资产，已播内容固定 revision；整课尚未固定就显示“后续准备中”。媒体管线必须能把实际生成片段和 OpenAI 音轨保存下来；如果所选视频服务不能可靠导出／捕获该输出，G3 不通过，先调整媒体路线。

重复整段、章节、单句及 A-B 均读固定资产；允许音高保持的减速和前一句重播。更换语速或对白的生成变体建立新版本，不覆盖旧材料。部分失败只重试失败块，刷新按幂等键回到同任务，不重复开多个付费流。

### 5.3 动态对练

切换时释放被用户接替的角色，保留老师／店员等对方身份及声音。确认可播放后 AI 先说；准备开口才申请麦克风。拒绝可继续听和选意思，不能作口语失败。

轮次由服务端控制，客户端上报实际播放完成、可见性、手动说话与思考按钮。默认等待6秒，可选10–15秒；最多两次提示后回示范。用户可关闭主动提示，偏好持久保存；关闭后AI仍先开场，但不在静默中自动提示，手动求助仍可用。已知思考／说话状态不触发帮助；未声明静默按等待偏好执行，不宣称能读心。网络重连或后台暂停禁止错误触发。

用户可以合理回答、拒绝、要求变体或改目标；模型工具提出转向，服务端验证后更新目标，必要时一个简短选择确认。一次只增加当前能力可处理的条件。教练介入有提示，结束回原角色；小错少量延后反馈，意思不明先澄清。

### 5.4 回顾、校准和运营

回顾先听懂什么，再看表达证据，分别记录理解、表达、追问、澄清和帮助。至少含一个未原样示范的问法或条件；不能提前泄露跟读答案。用户能点句子报错，关联模板可追查，修正建立新版本，正在重复的旧材料保持可识别状态。

**90 份校准样本在 M1 获取，不等待 M3 的 24 人试点。** M0 准备研究同意、抽样和标注协议；M1 的内部可用版本在独立授权下采样。覆盖三种起点、拟上线辅助语言、四级完成程度；至少两人独立标注，按说话者及场景隔离开发／留出，留出≥30%且至少27份。样本格缺失先补格，不用更多同一人的相邻片段补足覆盖。

自动任务结果在留出上完全一致率≥85%，且完整答案提示被判独立、未表达意图被判完成均为零，才可放行相应范围。未达到时仍可开发具体练习回顾，但任务 T26、G5 评分项及 M1 出口保持未通过；不能以关闭评分功能宣布原规格已全部交付。24 人 G6 使用另行设计的基线／延迟任务，不拿校准数据重复证明有效。

## 6. M2：扩展到完整首版

将六个样例和36个种子建立为可版本化内容资产；每项记录来源、适用场合、意图、难度、动作、本地表达与审核人。家校、工作、生活三类均要有列表外任务。新任务不能要求工程改代码或增加唯一正确台词。

辅助语言组合按 D02 决定，翻译覆盖未登录、OAuth返回、权限、失败、重连、词义、反馈和删除；未审语言不能标正式支持。中文可用不能替代非华语零基础验收。成人友好字体、按钮、音频提示和减少动画在目标手机检查。

固定回归规模：30 个目的×3种起点=90 个场景输出；每个覆盖5类交互=450条基础检查；至少12个独立留出新意图；再加 spec §13.3 的专项。六样例各10次 AI 主动开场、每样例5个合理转向；长停顿、思考、手动录音、后台、拒绝权限、重复取消和回声必须覆盖。

目标设备至少 iOS Safari、Android Chrome 各一台较旧且仍支持的常见手机，桌面作补充；真实设备／OS／网络版本写入清单。每种目标设备和网络至少100轮回应，每设备3次20分钟会话，记录第1、5、15分钟延迟；外放和耳机分别测试。微信授权和微信内媒体分开判定，外部浏览器接续必须实录。

所有失败和重试计入分母。T35 汇总全部73条验收和 G1–G5；M0 证据只有在相同相关版本／环境下才可复用，变更部分必须重测。F15 的首版验收是无积分／社交门槛且范围隔离；通知功能尚不实施。

## 7. M3：学习与融入试点

T36 先交付有访问控制的试点发布方案，包含准确变更、费用、数据流、检查、停止招新、结束媒体会话和回退到已验证版本的办法；获得该环境授权才部署。T37 在外部联系授权及参与者同意后开展24人招募，不发送未经批准的邮件或消息。

分组沿用 spec §14：12 名近零基础（华语6、印尼语3、日语3）；另12名听说断层／本地适应（华语6、非华语6）。这是 D02 的建议组合，不是人口代表性配比；语言变更先更新采样和对应审核，不能以有基础者替代零基础名额。

流程为自愿基线观察、独立进入、3次分散练习、48–72小时后的平行未见任务、压力与现实尝试回访。基线／延迟观察不成为所有用户的入门考试。口语可选，未开口记未观察，不能以此排除出报告或悄悄补人。

预设观察目标：独立进入≥20/24，近零基础≥10/12；至少16/24在预设关键任务提升一档。听力为主结果，表达按实测记录；分母、未完成、退出、未提升、下降、各母语与起点均报告。无对照的24人不能证明因果有效，也不能据此宣称消除焦虑。

T39 根据失败修正并做针对性复测；保留原结果，不用反复测试同一熟题凑“通过”。修复涉及语音、媒体、评分或隐私时列出受影响G/F，T40只能引用与最终候选版本一致的重验记录，不能沿用已失效的T35放行。T40 输出 G6 决策及首版交接：继续、修正或缩小验证范围。公开上线、定价和营销另行批准；后续提醒、模拟呼叫和积分另写规格，不把读写加回路线。

## 8. 成本、数据和故障处理

### 8.1 预算包的可执行格式

T06 按以下表头逐项填写真实单价后提出额度建议：供应商／端点、单位、价格核查日、样本数、预计输入输出、活跃角色秒数、录存／传输、成功与失败重试、预计费用区间、用户批准上限、实际费用、终止证明。当前所有金额为**待核价／未授权**，不能把空白理解成无限预算。

X2 是30对声音；X3 为三条候选各30新任务和10连续课件；X4 为最终候选100新启动及双角色、关闭等专项；长会话／评分／M2矩阵／M3另行分配预算，不假定一次小额实验涵盖整个项目。计费按真实供应商 usage 分类别结算，不能把不同 token 混乘一个价格或编造每分钟固定成本。

付费实验建议单场最多20分钟、初始并发1个学习／实验会话、失败自动重试最多1次；一个会话可以包含两条独立角色流，费用预留按实际流数计。这是实验保护参数，不是正式产品限额。每次调用前按受限token、最长媒体时长、角色数和重试预留最大额度，已结算加在途预留不能超过批准上限；不能界定费用暴露时不自动发起。结束后按账单冲销预留，取消仍追踪在途费用。后台超过建议30秒结束付费视觉流。重听固定材料不重新调用生成；付款、扩容和报酬需各自已在授权范围内。

### 8.2 故障恢复表

| 故障 | 用户行为与工程动作 | 完成证据 |
| --- | --- | --- |
| 无麦克风／无法自动播放 | 先说明并等播放手势，继续听；开口前才申请权限 | 真机拒绝后回听，再次启用录音 |
| 模型拒绝／结构或语义不合格 | 显示可理解失败；只追问必要细节或给同目的简化选择；不发不合格媒体 | 失败样本、拒绝路径、无虚假完成状态 |
| 首段视频超30秒 | 同场景听力先行或取消／重试，明确未生成新视频 | 原任务幂等，视频失败仍入分母 |
| 后续镜头失败 | 保留已完成章节，只重试失败部分 | 旧片段 hash 不变，不重复计费生成成功块 |
| 用户打断／改目标 | 取消模型、权威音轨、视频缓冲；旧版本响应丢弃 | 客户端停止时间及服务端取消／版本记录 |
| 切后台／退出／断网 | 停提示，按阈值释放媒体；恢复确认任务，AI重新引导 | 10次离开和10次断线无遗留付费会话 |
| 内容错误 | 句子工单、关联模板定位、暂停或新修订，保留可解释旧版本状态 | 用户下一次访问能看见真实处理状态 |
| 删除账号／记录 | 立即撤销访问；删除排程覆盖存储、日志和备份 | 两账号隔离测试、删除工作记录、到期证明；加速时钟测试不能冒充已等完30天 |

### 8.3 数据流与最小权限

登录身份由服务端验证；客户端只持短期会话凭证。PostgreSQL 保存 owner、目标与 revision、结构化观察和工单；音视频放私有对象存储，访问使用短期授权；分析日志不收集完整家庭对话。微信 openid 按应用隔离，跨身份合并必须同时验证两个身份。

服务地区边界在服务端申请生成／会话前统一检查，和选择微信或Google无关；只采集判断可用地区所需最少信号，不要求GPS定位或推断国籍。地区不支持或无法可靠确认时给辅助语言说明，不提供绕行办法。T07验证入口，T13验证直接业务请求也不能绕过。

删除时撤销访问能力并终止播放连接。私有媒体读取采用可撤销的授权检查／代理，不仅依赖仍未到期的对象存储签名URL；验证旧链接及重连请求不能继续取回已删除场景。已下载到用户设备的数据无法远程追回，不作该承诺；后端副本仍按删除排程处理。

用户麦克风进入受控媒体链与 OpenAI；视频供应商原则上仅收合成输出音轨、参考图与脱敏提示。LiveKit房间订阅、录制／转写、供应商日志和删除必须逐项核实。T06建数据流草图，T09/T10完成M0真实观察所需的最小端点核验和告知，T24复核完整产品，T36发布试点版本最终告知；各阶段真人参加前均须收到当时适用说明。默认关闭长期原始录音和研究采集。

## 9. 闸门与责任分工

| 闸门 | 初次任务／最终复核 | 责任角色 | 通过记录要求 |
| --- | --- | --- | --- |
| G1 登录 | T07／T27、T33、T35 | 工程 + 账号主体负责人 | 同机实录、权限／回调、取消、重试、接续、隔离 |
| G2 声音 | T08／T30、T35 | 本地英语审查者 + 教学负责人 | 五维分数、样本、复核和角色连续性 |
| G3 视频 | T09–T10／T15–T17、T33、T35 | 媒体工程 + 真机测试 | 首段与整课分开、双管线、动作、固定重播、终止 |
| G4 零基础 | T11／T31、T35；M3继续观察 | 产品研究 + 母语审查者 | 华语及非华语独立操作、无读写门槛、可听求助 |
| G5 会话／反馈 | T18–T23、T26、T28／T32–T35 | 工程 + 两名教学标注者 | 状态与真机回归、留出校准、明确未知结果 |
| G6 学习／融入 | T37–T40 | 用户研究 + 教学 + 产品负责人 | 未见任务、延迟结果、分组失败、局限和决策 |

AI可承担实现、测试脚本、资料整理和初稿；本地真实性、真人许可、教学标注和研究结论不由工程代理自己作最终裁判。具体人员、报酬与时间尚未落实，T06/T36形成具名清单；缺角色不能用AI自评冒充人工放行。

## 10. 每批怎样推进和验收

开工前读取四份文档、确认批准范围和实际环境；只取批准批内依赖已完成且外部条件齐备的任务。先写行为考题并验证失败，锁定考题再实现；验证通过后记录证据、复核、提交与状态。纯文档／样式任务用相应审阅，不为数字好看增加无效测试。

测试与外部证据是两套状态。自动套件退出码0只说明所执行的自动检查通过；人工／付费／真机项未测仍为空，不能使用空测试、全跳过或伪造 fixture通过真实能力。每批独立只读评审，至少提出一个测试盲区或明确说明未覆盖范围。

初始化前已审查脚本，并以`.gitignore`排除研究原件、PDF、临时文件和密钥。用户已授权的Git、`.loopwork`、`.codex`和AGENTS已建立；真实Stop回执和PostToolUse审计已经核实，详细边界见docs/setup-status.md。当前skill通过`progress.py commit red|green|note`登记存档，轮末hooks核验并提交；下一轮收到红存档回执后才实现，收到绿存档回执才确认任务存档成功。不直接执行实现提交、不手动重复增加轮数。尚无实际hook回执时不能进入无人值守循环。

持续三次失败先诊断，最多再验证一次；仍失败记录 `BLOCKED.md` 的具体恢复条件。批末／连续两项受阻时做 N 对 M 对账，保留下一步。用户已批准的普通可逆内部选择由工程自行处理，不为每个组件重问。规格冲突或性能门槛需要改变时先给有证据的变更方案。

## 11. 技术附录：实现契约

本节是拟实施的项目接口，不是声称供应商原生提供这些业务 API；路径可在不改变行为的前提下调整并记录。

### 11.1 建议目录

```text
apps/web/              手机界面、OAuth 回调、业务 HTTP 接口
services/media/        LiveKit worker、轮次控制、视觉输出与关闭
contracts/             场景、媒体、事件、评估的版本化 JSON Schema
content/               审核后的种子、表达、图卡与辅助语言
tests/                 单元、合约、浏览器、故障与版本回归
docs/evidence/         运行记录及脱敏证据索引，敏感原件另受控保存
scripts/               本地启动、验证、实验清单和计费汇总
```

这些目录当前未建立为应用。第一批不需要先架 Redis、微服务网格或完整后台管理。持久生成 worker 用任务记录与租约／心跳恢复，按同一幂等键领取；M1 再以实际并发测是否需要专门队列，不能先假设生产吞吐量。

### 11.2 业务接口与授权

| 拟定操作 | 输入／返回 | 必须验证 |
| --- | --- | --- |
| `POST /api/scenes` | 用户目的、语言、支撑；返回 draft 和确认卡 | 已验证用户；不采集不必要实名；来源／未知事实 |
| `POST /api/scenes/{id}/confirm` | 当前 revision、确认目的；返回 goal_revision | owner、版本冲突、幂等键；不以旧确认覆盖新目的 |
| `POST /api/scenes/{id}/lessons` | 时长与速度；返回 generation_job_id | 费用授权、并发、私有归属；job_id不等于ready |
| `GET /api/jobs/{id}` | 分段状态、可播放资产、失败原因 | owner、短期媒体地址、准确 partial／complete |
| `POST /api/jobs/{id}/cancel` | 幂等取消 | 停新任务、追踪已提交任务、实际供应商结束 |
| `POST /api/practice-sessions` | lesson_revision、参与位、只听／对练 | 发布前权限；短期受限RTC凭证；禁止订阅他人音轨 |
| `POST /api/practice-sessions/{id}/events` | event_id、seq、response_id、goal_revision、状态事件 | owner、顺序／重复校验、允许状态转移；不信任客户端评分 |
| `POST /api/content-issues` | 句子与revision、问题类别 | 用户能访问该课；最小证据；关联版本可追查 |
| `DELETE /api/scenes/{id}`、`DELETE /api/me` | 删除范围；返回删除回执 | 撤销访问、停止会话、排程所有数据副本及明确保留说明 |

OAuth 回调按最终提供商官方流程实现 `state`／重放防护和签名校验，支持时采用相应推荐保护，不把 Google 与微信参数机械通用。业务 cookie、回调和跨浏览器接续不得泄漏令牌；具体库在真实资格核验后确定。

### 11.3 媒体和状态边界

网页业务服务负责目标、持久记录和权限；媒体 worker 是实时轮次唯一控制者。两者只通过版本化事件更新。示范 director 向两个独立音频输出管线逐句送固定音轨；练习只保留对方的实时音轨。音频和视频发布来源一致，前端只订阅获准轨道。

场景状态、每个片段状态、整课状态、角色创建状态分开；沿用 spec §11.2。所有事件包含 `event_id / session_id / response_id / utterance_id / goal_revision / seq` 及发生时间；非适用字段可为空，不伪造时间。跨设备重新连接以服务端最后确认状态为准，不按客户端随机重放。

### 11.4 验证入口合同

T01 创建后才存在的命令合同：根目录 `npm run dev` 启动本地网页；`npm run verify:offline` 执行已定义单元／合约检查和构建；`npm run verify:e2e` 执行浏览器用例；媒体加入后纳入 `python -m pytest tests/media`。实际路径、依赖、启动流程写入届时的 README，锁定并跑一次干净环境复现。

若启用 Loopwork，`bash .loopwork/hooks/verify.sh` 必须调用项目真实套件，并对不存在、零测试或意外全跳过返回失败；当前该脚本并不存在，不能在本轮报告“全绿”。真实服务实验单独人工触发，默认测试命令不得联网计费。

证据清单最少字段：`task_id / acceptance_ids / gate / code_commit / content_version / model_snapshot / environment / started_at / sample_count / pass_count / failure_count / evidence_paths / reviewer / verdict / limitations`。敏感原音频不进 Git；存授权和受控位置引用。所有“通过”可反查原始记录，所有“未测”明确显示。
