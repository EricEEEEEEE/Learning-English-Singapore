# Learning English in Singapore · 项目规则

版本：1.1 · 2026-09-11 · 状态：已批准初始化与B1；初始化已执行，等待当前会话hooks接通验证，开发任务0/40。

本文件约束后续实现和验收，依据最终 `spec.md` v0.2。配套文件为 `plan.md` 和 `tasks.md`。文档中的“必须”是交付条件，不代表有关功能或保护机制已经运行。

## 1. 基线与范围

1. `spec.md` 是产品需求和验收依据；`rules.md` 是边界；`plan.md` 是实施路径；`tasks.md` 是进度清单。冲突先指出并修正文档，不能挑较宽松的一项执行。
2. 文档编制后，用户明确批准“同意初始化项目及上述项目级配置，按计划开始第一批”。授权包括本项目初始化及B1的T01–T06；不含付费调用、真实账号申请、部署或招募。已批准范围不重复索取许可。
3. 本次依据的规格 SHA-256：`717c700682b5803c14673204512d06cef7158941b487002b21311b7173dff843`。实施前核对；如变更，重新做受影响的任务和验收映射。
4. 首版范围为 M0–M3。M4 只是后续方向，另行确定规格。当前全部开发任务未勾选；文档完成不算产品完成。
5. 仅在本项目和获准临时目录内写入；保留既有研究、来源、PDF 和其他人的改动。不得顺手删除、重命名、重排旧文件或修改全局／项目身份规则。

## 2. 产品不可越过的边界

| 编号 | 必须保持的行为 |
| --- | --- |
| R01 | 面向在新加坡等支持地区的所有成人新来者；明确覆盖华语与非华语的近零基础使用者。不用族群比例推导用户需求或学习水平。 |
| R02 | 听为第一，说为第二。阅读、拼写、写作训练与考试不在范围；文字、图卡、字幕仅作可选理解支撑。 |
| R03 | 自然逐题选择了解用户，不用试卷、倒计时、分数或强制朗读。跳过、设备失败和口语未观察必须保留为未知。 |
| R04 | 不要求真人互动、开摄像头、加群、固定课表或公开录音。进入对练由 AI 先开场；只听仍是有效使用方式。 |
| R05 | 母语可以描述需求、听操作说明、求助和理解重点词；角色交流以英语为主，帮助后回到情境。不惩罚求助。 |
| R06 | 推荐场景只是起点；用户可提出列表外任务、合理回答和转向。不能以固定正确句、改标题或随机堆分支冒充个性场景。 |
| R07 | 两位可区分的 AI 卡通角色示范；切对练保持对方身份和声音。反复听同一不可变版本，不每次重新随机生成。 |
| R08 | OpenAI 提供语音；允许外部图像、视频模型。生成式视频为主路线；纯音频、静态图、程序动画只能作为明确标注的降级或对照。 |
| R09 | 使用“新加坡英语”“本地口音／方言与表达”。尊重所有口音，不把模仿某种口音或消除母语口音作为通关条件。历史来源原题不改写。 |
| R10 | 首页、帮助、引导、练习、反馈及未来营销遵守 spec §6.5 的尊重声明；不生成族群贬低、劣质口音排名或身份歧视。 |
| R11 | 仅微信、Google 两个账号入口，都向合资格用户开放。不能暗删微信、加邮箱入口、以电脑扫码冒充手机同机登录。 |
| R12 | 不承诺十遍学会、消除焦虑、正式英语认证或已证明学习效果；不把使用时长、跟读、提示后回答当独立掌握。 |

## 3. 技术与教学规则

1. 分级执行 `placement_rules_v1`，按 spec §6.2 的独立证据、前置档和冲突规则判断；LLM 不直接猜完整能力。无证据为 `unknown`，暂用入门内容，不等同能力 L0。
2. 一个会话只有一个轮次控制者。实际播放结束才开始等待；`thinking`、说话中、手动录音、后台、权限与网络故障时禁用沉默提示。每次沉默最多两次帮助，再回示范；回应到达取消排队提示。
3. 双角色使用独立输出管线或实测合格的串行切换；不能把两个默认 AvatarSession 挂在同一个被替换的音频输出尾部。导演控制台词，不能让两套自主代理互聊。
4. 全程一条权威 OpenAI 音轨，禁止视频端默认 TTS 替换或重复播放。声画缓冲计入真实延迟。建角、提交任务、首帧、可学习首段、完整固定课件是不同状态。
5. 用户打断／取消须停止模型响应、声音、视频和未播放缓冲；旧 `response_id / goal_revision` 不得回写或复播。供应商结束状态必须核实，关闭网页不算终止计费。
6. 生成结果须过结构和语义检查，拒绝、截断、错角色、未知事实均有明确处理。官方事实、用户提供信息、虚构练习对白分开记录。模型不能代表实际学校、医生、雇主或政府作决定。
7. 本地表达的“已验证”需对应审查记录；自由生成不能声称逐句经真人审过。关键时间、数量和方向由核验图卡表达，不能信任视频随机生成的字或动作。
8. ASR 文字不能单独支持音素错误判断。听不清先澄清；轻微错误默认稍后纠正一个重点。反馈保留支撑程度和不确定性。
9. 模型名、SDK、插件及资产许可在首次接入时核实并锁定；模型／插件升级先做相关回归。不把演示、星数、论文或宣传时延当项目验收。

## 4. 数据、费用与外部动作

| 动作 | 规则 |
| --- | --- |
| 只读核查、已批准批次内可逆实现、常规验证 | 自主完成；发现具体越界再提出，不为每个内部选择追问。 |
| 付费 API、GPU、云资源、账号付费或审查报酬 | 先给出供应商、用途、样本数、单价核查日、硬上限、失败重试和停止办法；取得明确预算后执行，不超额续费。 |
| 真实 OAuth 配置与测试环境发布 | 列明主体、域名、回调、访问范围、变更与回退，取得该环境授权；开发批准不自动包含外部部署。 |
| 招募、联系审查者、邮件／消息、发布研究材料 | 先有外部联系授权；参与研究、录音、供应商处理、公开展示分别授权，不能捆绑。 |
| `.codex` 配置、AGENTS.md、全局规则或自动循环 hooks | 本次不写；先列出准确路径和影响，取得相应明确授权。不能直接运行会批量生成这些文件的初始化脚本。 |
| 删除现有文件、破坏性 Git、生产迁移、公开发布 | 单独确认具体对象；生产变更另附检查和回退。禁止强推、清库或关闭安全检查来赶进度。 |

付费请求发起前按可约束的最大 token、媒体时长、角色流数及重试预留额度；在途预留与已结算合计不得超过批准上限。不能确定最大暴露费用时不自动发起，先限制请求或取得该具体风险的授权；不能只在事后账单超额时停止。

长期密钥只在服务端受控配置中，示例只含变量名。测试、截图、日志和提交不得包含密钥、原始身份令牌或私人对话。课件按 owner 鉴权；模型工具不得访问任意账号或任意数据库。

默认不长期保存原始练习录音。研究和可选回听分别征得同意；普通使用不能以同意研究为前提。视频端仅接必要的合成声音、参考图和脱敏提示，禁止默认订阅用户所有音轨。D06 未核实前，外部技术验证只用虚构任务与获授权素材；不能招真实用户进入该数据链。

产品删除覆盖数据库、对象存储、缓存、日志和备份；规格目标为即时失去前台访问、活动存储 7 天内、备份 30 天内过期，验证前不作为已实现承诺。`store=false`、本地不录音、新加坡部署均不代表供应商零留存或全部数据位于新加坡。

## 5. 测试、证据与改动纪律

1. 每条任务先核对验收编号与依赖。新增行为写可证伪测试，复现失败后再实现；样式和研究工作采用合适的截图、人工审核或实验记录，不写只镜像实现的测试。
2. 实现阶段冻结本任务验收测试及规格、规则。测试有错时停止实现，说明矛盾并回到测试编制阶段，保留修订前后依据；不得删失败断言、改阈值或跳过用例来变绿。
3. 离线自动测试使用明确标记的替身，不联网、不计费。真实模型、真机、当地审查和真人结果单独放行；离线通过不等于 G1–G6 通过。
4. 每个验收保存：任务／F 编号、版本、环境、输入、预期、实际、原始证据位置、执行者、结论与局限。自动命令记录 exit code；人工结论记录人数、样本分母与分歧。
5. 性能必须含失败、重试和冷热条件，记录实际可听／可见时间。不能把供应商 job ID、首个无语义帧或缓存欢迎语计作新生成课件成功。
6. 评分校准集和学习效果试点独立；留出按人及场景隔离。未过校准就显示具体回顾与“尚不能判断”，不能称确定性评分已验收。
7. 一项完成须同时具备行为证据、相关回归、无未处理阻断、独立复核和存档。Git 尚未初始化时不伪造 commit；没有人工／真实验证时不得勾选相应任务。
8. 当前Loopwork采用代存档：通过 `progress.py commit red|green|note` 登记，由轮末hooks核验后提交；以下一轮真实commit回执为准。红测试存档到账后才进入实现，绿存档需验证通过；不得手动重复递增轮数。初始化例外由已审查安装脚本完成，事先忽略原始研究、媒体、临时文件和密钥。未经授权不push。

## 6. 分批、阻塞和恢复

首批 6 项；后续按 `tasks.md` 的 5–6 项分批，获准批次内按依赖取第一条可执行任务。连续两项受阻或到批末即对账；不得跨到尚未批准的批次。★ 表示可展示节点，是否结束批次以任务表“批末”标记为准。

同任务连续失败三次先诊断根因，只再做一次有依据的修正验证；仍失败记录问题、尝试、证据、受影响任务与恢复条件，继续已授权且无依赖的工作。付费实验另受预算硬上限控制，不为凑样本自动补付费请求。

实施时才建立 `JOURNAL.md`、`BLOCKED.md`、`docs/evidence/` 和运行状态。记录批准原话、当前批、已完成／受阻项、下一项、模型和内容版本。恢复先读文件和实际状态，不凭对话记忆猜进度。

独立评审至少检查：73 条验收覆盖、需求红线、失败路径、实际证据以及测试会漏掉的一个用户行为。批末按“预期 N 项 / 已证实 M 项”逐项对账，未通过不写“基本完成”。

Git、`.loopwork`及项目hooks文件已安装，首次存档已核验；当前会话尚无自动进度卡／审计／代存档回执，不能称hooks已接通。外层无模型自检验证了沙箱能力，但不替代实际会话加载验证。接通前不启用无人值守循环，不伪造保护或任务完成证据。
