# T03 独立评审

判卷员：`t01_review`（只读 Loopwork reviewer）。结论：**通过，仅限 T03 本地原型实现**。真实绿档仍须 Stop 回执。

## 合规与验收

N 对 M：声称 1 条，核实 1 条——T03 逐题选择、12 核心＋最多 4 附加、跳过、回退、暂停恢复与支撑历史均对上。关联 T02 刷新标题回归已修复。

独立运行 `bash .loopwork/hooks/verify.sh`：exit 0，102/102（96 浏览器＋6 状态），见 [日志](independent-verify.log)。相对红档 `cee8b9556c`，tests/spec/rules/hooks 无改动；未见真实 OAuth、付费调用或发布。

## 质量

未处理 🔴 为 0。已修复损坏快照重新预览时覆盖旧记录的问题；主代理补查 exit 0，12/12 布局、3/3 损坏记录保留、录像路径通过。判卷员已复核脚本、结果和截图。

⚪ 音频与内容仍为明确标识的占位；所有回答保留 `simulation`、`eligibleForAssessment=false`，不构成真实能力证据。

## 作弊清单

1. 空转修复：未命中，UI 与状态均有实质实现。
2. 断言放水：未命中，封存断言未变。
3. 断言消失：未命中，102 项全部实际执行。
4. 吞异常：未命中，存储异常显示提示并保留本次操作。
5. 抑制检查：未命中，未新增忽略或跳过检查。
6. 假重构：未命中，调用链与类型检查、构建一致。
7. 查表蒙混：未命中，常量表提供题目与翻译，历史由事件实际更新。
8. 功能孤岛：未命中，语言、显示设置、恢复与答题共用状态，交叉路径通过。

## 考题盲区与评审边界

损坏存档后重新预览若覆盖原始 bytes，冻结的 102 项仍可能全绿；本轮补查证明修复，下一次 T04 test-writing 应固化回归。

判卷员未完整播放录像，只核脚本、生成记录及截图；未覆盖母语审查、真实手机、屏幕阅读器、真实音轨和成人可用性研究。主代理随后完整解码 190 帧（exit 0），抽查 2.5/4.5/6.8 秒图片，见 [录像核验](video-check.json)。两次早先 FFmpeg 参数尝试因缺少 filter/muxer 失败，原日志保留；最终使用受支持的 image2 完整解码。

给用户的一句话：资深工程师会通过本轮原型评审，真实听力能力尚未验收。
