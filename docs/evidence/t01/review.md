# T01 独立只读评审

评审者：`/root/t01_review`（Loopwork只读判卷员）。结论：**通过，仅限T01实现**。
预期1项／核实1项：本地手机入口、诚实演示披露、验证入口及干净复现对上。
独立运行 `bash .loopwork/hooks/verify.sh`：exit0，12/12；原始记录见
`independent-verify.log`。T02–T06未计入。

规格hash一致；tests、spec、rules、hooks、AGENTS与.codex相对红提交9008f9995e
无变更。未发现密钥、真实OAuth、模型调用或数据收集。

原先缺少的verify:offline／verify:e2e入口、过期接续状态和截图路径均已修正，
无当前正确性或安全阻断。红阶段exit254只证明应用前置缺失，不能称12项UI断言跑红。

| 作弊清单 | 结论与依据 |
| --- | --- |
| 空转修复 | 未命中：实际页面与状态交互已实现 |
| 断言放水 | 未命中：封存tests无diff |
| 断言消失 | 未命中：断言完整保留 |
| 吞异常 | 未命中：应用无空catch |
| 抑制检查 | 未命中：无测试跳过或应用检查豁免；Next自动添加的skipLibCheck仅作用于依赖声明 |
| 假重构 | 未命中：组件引用、类型检查与构建一致 |
| 查表蒙混 | 未命中：静态文案描述未来功能，没有伪造服务能力 |
| 功能孤岛 | 未命中：说明展开、ARIA和显示共用同一状态 |

盲区：封存用例使用focus()/click()，不能自动覆盖Tab导航与真实tap。主代理已补充
本轮手工证据（manual-checks.json），后续测试编制阶段应补持久回归，本轮保持考题冻结。
评审者未亲自执行这些补充操作；未验收真实手机Safari／Android、外部服务或后续任务。

视觉核对：手机与桌面截图的中文文案、字阶、白底深绿配色、按钮/图标、留白与边界清晰。
展开态说明不遮挡其他内容；320px展开状态无横向溢出。用户限定离线原型，本轮使用原生
排版和SVG，无Image Gen概念图，因此不声称与生成概念图逐像素对齐。

T01 green由Stop hook落档；本文件的首个Git提交即该轮实现存档，回执到达后才确认存住。
