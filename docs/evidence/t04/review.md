# T04 独立评审

判卷员：`t01_review`（只读 Loopwork reviewer）。判卷结论：**通过，仅限 T04 原型与确定性规则**。绿存档仍须真实回执。

## 合规

N 对 M：声称1条，核实1条——T04证据分级、前置档、冲突与共同预算、模拟隔离、帮助调整和恢复均对上。

独立 `verify.sh` exit 0，167/167（132浏览器＋35 domain），见 [日志](independent-verify.log)。相对红档53fdf9ac，冻结考题、规格、规则及hooks未变；未见付费调用、真实OAuth或发布。

## 质量

未处理🔴为0。已修复累计帮助/故障遗漏、设置文本分隔，以及存储失败时仍暗示落盘的文案。

主代理补查exit 0：7项规则检查、12/12四语大字布局、4/4损坏设置保留及实际Tab→Enter/Space路径通过。判卷员复核脚本、结果和截图；未把解析器拒绝坏字符串当作存储bytes保留证据。

⚪ 分级fixture只证明规则计算，真实声音、措辞区分与成人校准仍未验证。

## 作弊清单

1. 空转修复：未命中，规则、档案及UI均有实际实现。
2. 断言放水：未命中，冻结断言未改。
3. 断言消失：未命中，167项全部执行。
4. 吞异常：未命中，存储失败可见，读取失败阻止覆盖旧数据。
5. 抑制检查：未命中，未新增跳过或忽略检查。
6. 假重构：未命中，类型、构建及调用链验证通过。
7. 查表蒙混：未命中，证据经去重、排除、冲突和前置规则实际计算。
8. 功能孤岛：未命中，语言、附加题、手选设置与恢复共享状态并通过交叉验证。

## 盲区与自查

新增设置控件若退出Tab顺序，冻结键盘题直接focus仍可能通过；累计支撑映射也尚未冻结。当前补查已通过，T05 test-writing应固化两项。

未做真机、屏幕阅读器、母语内容审核或真实听力校准；绿存档仍须真实回执。

给用户的一句话：资深工程师会通过本轮原型评审，真实分级仍待校准。
